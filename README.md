# Ketometer Pro

Offline ketose-tracker (Visual Keto Log).

Open `index.html` in a browser. Single-file app: Today, History, weight, portion-based carbs.

Canonical branch: `main` (migrated from Claude Code branch names during Appie cleanup 2026-09-16).

Safety backup tags: `backup/pre-cleanup-2026-09-16--*`.
